<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SimilartyTextApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SimilartyTextApp" parent="Base.Theme.SimilartyTextApp" />
    <style name="buttonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/rounded_corner_primary</item>
        <!--        <item name="android:padding">5dp</item>-->
        <item name="cornerRadius">15dp</item>
        <item name="backgroundTint">@color/primary</item>
    </style>
</resources>